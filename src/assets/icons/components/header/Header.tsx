import React from 'react'

interface HeaderProps {}

const Header = (props: HeaderProps) => {
  return (
    <section className={'font-black text-center text-4xl mb-8 select-none'}>
      NBBANGCHIJA
    </section>
  )
}

export default Header
